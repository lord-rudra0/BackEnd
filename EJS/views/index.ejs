<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <h1>
        Hey bro
        <%= dayType %>
            <%= advice %>

    </h1>

    <ul>
        <% for(let i=0;i<fruits.length;i++) {%>
            <li>
                <%= fruits[i] %>
            </li>
            <% }%>
    </ul>

    <h3>
        <% if (data.seconds%2===0) {%>
            <ul>
                <% for(let i=0; i<data.bowls.length;i++) {%>
                    <li>
                        <%= data.bowls[i] %>
                    </li>
                    <% } %>
            </ul>
            <% } else { %>
                <p>No Item </p>
                <% } %>

    </h3>




    <% if (data.seconds%2===0) {%>
        <ul>
            <% data.bowls.forEach((fruits)=>{%>

                <li>
                    <%= fruits %>
                </li>
                <% }); %>
        </ul>
        <% } else { %>
            <p>No Item </p>
            <% } %>





                <!-- data -->
                <h1>
                    <%= data.title %>
                </h1>

                <p>
                    <%= data.seconds %>
                </p>

                <h3>
                    <% if (data.seconds%2===0) {%>
                        <ul>
                            <% for(let i=0; i<data.bowls.length;i++) {%>
                                <li>
                                    <%= data.bowls[i] %>
                                </li>
                                <% } %>
                        </ul>
                        <% } else { %>
                            <p>No Item </p>
                            <% } %>

                </h3>

                <p>
                    <%- data.html_content %>
                </p>

                <%- include("footer.ejs") %>
</body>

</html>